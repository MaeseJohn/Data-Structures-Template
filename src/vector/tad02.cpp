#include <iostream>

using namespace std;

#include "tporo.h"
#include "vector.h"

int
main(void)
{
  TPoro auxOK;
  vector<TPoro> a, b(5), c(10);

  cout << a.size() << endl;
  cout << b.size() << endl;
  cout << c.size() << endl;
}
